#!/usr/bin/env bash
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
if command -v python3 >/dev/null 2>&1; then
    exec python3 "$SCRIPT_DIR/schema-probe.py" "$@"
else
    exec python "$SCRIPT_DIR/schema-probe.py" "$@"
fi
